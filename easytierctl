#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# shellcheck disable=SC1091
source "${ROOT_DIR}/lib/platform.sh"
# shellcheck disable=SC1091
source "${ROOT_DIR}/lib/os.sh"
# shellcheck disable=SC1091
source "${ROOT_DIR}/lib/output.sh"
# shellcheck disable=SC1091
source "${ROOT_DIR}/lib/autostart.sh"
# shellcheck disable=SC1091
source "${ROOT_DIR}/lib/easytier.sh"

easytierctl_main "$@"
