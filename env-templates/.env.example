# 项目级公共环境变量示例
#
# 使用方式：
# 1. 执行 ./easytierctl init env 生成项目根目录 .env
# 2. 把这里的变量按需改成真实值
# 3. 之后 easytierctl 的所有命令都会自动加载根目录 .env
#
# 适用场景：
# - 下载 / 升级 EasyTier 时需要访问 GitHub API
# - 希望不每次手动 export 环境变量
# - 希望把公共变量和 create / join 配置分开维护
#
# 变量含义：
# - GITHUB_TOKEN：GitHub 个人访问令牌，用于提高 GitHub API 访问限额
# - GH_TOKEN：GitHub CLI 常用 token 变量名；如果和 GITHUB_TOKEN 同时存在，优先使用 GITHUB_TOKEN
#
# 推荐设置：
# - 如果你会频繁执行 download / upgrade，建议配置 GITHUB_TOKEN
# - token 只需要具备读取公开仓库 release 信息的能力即可
# - 如果你已经执行过 gh auth login，也可以不配置，由脚本自动尝试读取 gh auth token

# GitHub 个人访问令牌。
# 推荐设置：填写你自己的 token；如果当前环境已经 gh auth login，也可以留空。
GITHUB_TOKEN=

# 兼容 GitHub CLI 场景的 token 变量名。
# 推荐设置：通常留空；只有你习惯使用 GH_TOKEN 时再填写。
GH_TOKEN=
